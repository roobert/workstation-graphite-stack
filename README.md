# workstation-graphite-stack

## Deploy

```
# note: this will restart network-manager which could cause loss of network connectivity
ansible-playbook --ask-sudo-pass graphite.yml
```

# Access

Login using admin:admin at: http://localhost
